{
  "tokenCallbacks": [
    {
      "issuerId": "mockauth",
      "tokenExpiry": 315576003600,
      "requestMappings": [
        {
          "requestParam": "client_secret",
          "match": "secret",
          "claims": {
            "sub": "${client_id}",
            "aud": "https://daml.com/jwt/aud/participant/${participant_id}"
          }
        }
      ]
    }
  ]
}