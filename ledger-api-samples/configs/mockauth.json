{
  "tokenCallbacks": [
    {
      "issuerId": "mockauth",
      "tokenExpiry": 604800,
      "requestMappings": [
        {
          "requestParam": "client_secret",
          "match": "secret",
          "claims": {
            "sub": "${client_id}",
            "aud": "https://daml.com/jwt/aud/participant/${participant_id}"
          }
        }
      ]
    }
  ]
}