canton.participants.sandbox.http-ledger-api.server.port = 7575
canton.participants.sandbox.ledger-api = {
  auth-services = [{
    type = "jwt-jwks"
    url = "http://localhost:8080/mockauth/jwks"
  }]
}